server:
  port: 8086
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/Hualmss/dharma-back
          search-path: config-data
  application:
    name: service-config
